

Real life example

AutoCloseable is an interface class with close() abstract method, it also throws an exception

Then there is Closeable interface class that extends AutoCloseable class
Closeable also has a close() method which throws IOExeption

Then there is PcapHandle final class that incliments Clseable class
